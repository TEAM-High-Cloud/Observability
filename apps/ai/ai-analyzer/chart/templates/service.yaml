---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-analyzer.fullname" . }}
  labels:
    {{- include "ai-analyzer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ai-analyzer.selectorLabels" . | nindent 4 }}
